# Client Environment Variables
# For local development - no changes needed (uses Vite proxy)
# For production (Vercel), add this env var in Vercel dashboard:

VITE_API_URL=https://your-render-backend-url.onrender.com
